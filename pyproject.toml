[project]
name = "crewai-financial-researcher"
version = "0.1.0"
description = "crewAI crew that researches a company and writes a report"
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]>=1.15.18,<2.0.0",
]
